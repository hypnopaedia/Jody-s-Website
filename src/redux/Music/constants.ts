import { Player } from "./types";

export const DEFAULT_PLAYER: Player = {
    albumId: undefined,
    trackId: undefined,
    isPlaying: false,
    currentTime: undefined,
    duration: undefined,
};
